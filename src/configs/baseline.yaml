# src/configs/baseline.yaml

defaults:
  - _self_
  - model: multimodal_moe
  - datasets: shipear
  - trainer: train
  - inferencer: infer

hydra:
  run:
    dir: .
  job_logging:
    version: 1
    disable_existing_loggers: false
    formatters:
      simple:
        format: "%(message)s"
    handlers:
      console:
        class: logging.StreamHandler
        formatter: simple
        stream: ext://sys.stdout
    root:
      level: INFO
      handlers: [console]
  hydra_logging:
    version: 1
    disable_existing_loggers: false
    handlers:
      console:
        class: logging.StreamHandler
        stream: ext://sys.stdout
    root:
      level: WARNING
      handlers: [console]

# WandB writer
writer:
  _target_: src.logger.WandBWriter
  project: ShipEar-Multimodal-Classification
  entity: null
  enabled: false    # 设为 true 开启 WandB 上传